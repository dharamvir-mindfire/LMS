name: Deploy backend-csharp

on:
  push:
    branches: [main]
    paths:
      - "backend-csharp/**"
      - ".github/workflows/deploy-backend-csharp.yml"
  workflow_dispatch: {}

env:
  AZURE_WEBAPP_NAME: lms-d
  DOTNET_VERSION: "10.0.x"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Publish
        run: dotnet publish backend-csharp -c Release -o build/backend-csharp

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: build/backend-csharp
