{
  "name": "lms",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix app\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "app": "npm run start --prefix app"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
